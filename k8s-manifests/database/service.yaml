apiVersion: v1
kind: Service
metadata:
  namespace: k8s-noufa
  name: postgres-svc
spec:
  selector:
    app: postgres
  ports:
  - name: postgres
    protocol: TCP
    port: 5432
    targetPort: 5432
  type: LoadBalancer
